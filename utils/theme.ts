// utils/theme.ts
'use client'
import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#9c27b0", // Purple
    },
    secondary: {
      main: "#ff4081", // Pink
    },
  },
});

export default theme;
